{
  "name": "our-gymnastics",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev:client": "npm run dev --workspace=our-gymnastics-client",
    "dev:server": "npm run dev --workspace=our-gymnastics-server",
    "build:client": "npm run build --workspace=our-gymnastics-client",
    "build:server": "npm run build --workspace=our-gymnastics-server",
    "lint:client": "npm run lint --workspace=our-gymnastics-client"
  }
}